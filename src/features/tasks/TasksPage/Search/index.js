import Input from "../Input";
import { Wrapper } from "./styled";

const Search = () => (
    <Wrapper>
        <Input
            placeholder="Filtruj zadania"
        />
    </Wrapper>
);

export default Search;